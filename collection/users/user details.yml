info:
  name: user details
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
