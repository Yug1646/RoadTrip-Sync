info:
  name: update user details
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base}}/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
