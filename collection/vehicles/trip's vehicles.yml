info:
  name: trip's vehicles
  type: http
  seq: 1

http:
  method: GET
  url: "{{basetrips}}/:tripId/vehicles"
  params:
    - name: tripId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
