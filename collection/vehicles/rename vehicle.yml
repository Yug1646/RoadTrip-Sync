info:
  name: rename vehicle
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{basetrips}}/:tripId/vehicles/:vehicleId"
  params:
    - name: tripId
      value: ""
      type: path
    - name: vehicleId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
