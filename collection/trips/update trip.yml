info:
  name: update trip
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{basetrips}}/:id"
  params:
    - name: id
      value: "4"
      type: path
  body:
    type: json
    data: |-
      {
        "status":"active"
      }
  auth:
    type: bearer
    token: "{{userAToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
