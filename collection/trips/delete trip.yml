info:
  name: delete trip
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{basetrips}}/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
