info:
  name: create new trip
  type: http
  seq: 1

http:
  method: POST
  url: "{{basetrips}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
