info:
  name: "Join Code "
  type: http
  seq: 6

http:
  method: POST
  url: "{{basetrips}}/join"
  body:
    type: json
    data: |-
      // MAKE USER ADD THE TOKEN OF ANOTHER USER (NOT THE CREATER TOKEN)
      { 
        "joinCode": "3F20CD5F", 
        "type": "motorcycle"
      }
  auth:
    type: bearer
    token: "{{userBToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
