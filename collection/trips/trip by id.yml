info:
  name: trip by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{basetrips}}/:tripId"
  params:
    - name: tripId
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{userBToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
