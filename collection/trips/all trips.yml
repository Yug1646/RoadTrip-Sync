info:
  name: all trips
  type: http
  seq: 2

http:
  method: GET
  url: "{{basetrips}}/"
  auth:
    type: bearer
    token: "{{userAToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
