info:
  name: Welcome
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
