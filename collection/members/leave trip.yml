info:
  name: leave trip
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{basetrips}}/:tripId/members/:memberId"
  params:
    - name: tripId
      value: ""
      type: path
    - name: memberId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
