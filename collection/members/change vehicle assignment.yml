info:
  name: change vehicle assignment
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{basetrips}}/:tripId/members/:memberId"
  params:
    - name: tripId
      value: ""
      type: path
    - name: memberId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
