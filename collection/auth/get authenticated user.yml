info:
  name: get authenticated user
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/auth/me"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
