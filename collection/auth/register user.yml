info:
  name: register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username":"Jean Grey",
        "email" : "jeangrey@gmail.com",
        "password" : "jean@123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
