info:
  name: register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username":"Yug Jain",
        "email" : "yugjain@gmail.com",
        "password" : "Yain@1537"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
