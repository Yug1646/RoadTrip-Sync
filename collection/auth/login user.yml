info:
  name: login user
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email" : "jeangrey@gmail.com",
        "password" : "jean@123"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        bru.setEnvVar("token", body.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
