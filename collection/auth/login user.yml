info:
  name: login user
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email" : "yugjain@gmail.com",
        "password" : "Yain@1537"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
