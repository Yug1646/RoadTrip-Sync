info:
  name: all members location
  type: http
  seq: 2

http:
  method: GET
  url: "{{basetrips}}/:tripId/locations"
  params:
    - name: tripId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
