info:
  name: update location
  type: http
  seq: 1

http:
  method: PUT
  url: "{{basetrips}}/:tripId/location"
  params:
    - name: tripId
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "latitude": 40.74,
        "longitude": -76.00
      }
  auth:
    type: bearer
    token: "{{userAToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
